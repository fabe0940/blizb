class entity(x, y)
	method render(w)
		WAttrib(w, "fg=#FFFFFF");
		FillRectangle(w, x, y, 10, 10);
	end

	method process()
		write("entity:process");
	end

initially
	if /x then x := 0;
	if /y then y := 0;
end
